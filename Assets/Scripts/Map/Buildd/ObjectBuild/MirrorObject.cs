using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class MirrorObject : MonoBehaviour
{
    //  void Update()
    // {
    //     // 오브젝트의 Y축 방향 (로컬 축에서 Y축이 향하는 방향)
    //     Vector3 xAxisDirection = transform.right;

    //     // 월드 좌표계에서의 기준 축 (여기서는 세계 좌표의 Y축을 기준으로 각도를 구함)
    //     Vector3 worldUp = Vector3.right;

    //     // 두 벡터 간의 각도를 계산
    //     float angle = Vector3.Angle(worldUp, xAxisDirection);

    //     // 결과 출력
    //     Debug.Log($"Y-axis direction: {xAxisDirection}, Angle with world Y-axis: {angle} degrees");
    // }




    // private void OnCollisionEnter2D(Collision2D other){
    //     if(other.gameObject.TryGetComponent(out TEST_Projectile component)){
    //         component.dir = ReflectDir(component.dir);
    //     }
    // }



}
